{
  "name": "safari-tras-las-sierras",
  "version": "1.0.0",
  "description": "Aplicación para gestión de Safari tras las Sierras - Rally en Valle Fértil, San Juan",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../api && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "vercel-build": "cd frontend && npm install && npm run build"
  },
  "keywords": ["rally", "safari", "san-juan"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

